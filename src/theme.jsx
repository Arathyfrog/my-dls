// src/theme.js
// import { ThemeProvider } from 'styled-components';
// import colors from './tokens/color';

// const theme = {
//   colors,
//   // Add other tokens like typography, spacing, etc.
// };

// export const Theme = ({ children }) => (
//   <ThemeProvider theme={theme}>{children}</ThemeProvider>
// );
